# Optional: copy to .env. Defaults work without this file.
PORT=2567
# Configure the local admin using npm run admin:setup.
# Never put real credentials here. /login rejects tunnels and remote clients.
ADMIN_USERNAME=
ADMIN_PASSWORD_HASH=
# DATA_DIR=.data
