# Optional: copy to .env. Defaults work without this file.
PORT=2567
# Local debug only: host can type ADMIN! to unlock test controls.
# npm run share always disables these controls.
ENABLE_DEBUG_ADMIN=0
